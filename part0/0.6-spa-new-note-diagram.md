```md
```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save

    browser->>server: POST /new_note_spa
    server-->>browser: JSON response 

    Note right of browser: UI updates without page reload     




